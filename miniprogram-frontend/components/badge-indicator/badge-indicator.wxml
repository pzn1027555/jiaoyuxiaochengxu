<!--components/badge-indicator/badge-indicator.wxml-->
<view class="badge-indicator badge-{{size}} {{customClass}}" wx:if="{{visible}}">
  <text class="badge-text" wx:if="{{count > 0}}">{{count > 99 ? '99+' : count}}</text>
</view>


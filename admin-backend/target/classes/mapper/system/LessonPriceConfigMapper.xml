<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.education.admin.modules.system.mapper.LessonPriceConfigMapper">

  <resultMap id="BaseResultMap" type="com.education.admin.modules.system.entity.LessonPriceConfig">
    <id column="id" property="id" jdbcType="BIGINT"/>
    <result column="price" property="price" jdbcType="DECIMAL"/>
    <result column="currency" property="currency" jdbcType="VARCHAR"/>
    <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
    <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
  </resultMap>

  <sql id="Base_Columns">id, price, currency, create_time, update_time</sql>

  <select id="selectLatest" resultMap="BaseResultMap">
    SELECT <include refid="Base_Columns"/>
    FROM lesson_price_config
    ORDER BY id DESC
    LIMIT 1
  </select>

  <insert id="insert" parameterType="com.education.admin.modules.system.entity.LessonPriceConfig" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO lesson_price_config (price, currency, create_time, update_time)
    VALUES (#{price}, #{currency}, NOW(), NOW())
  </insert>

  <update id="updatePrice">
    UPDATE lesson_price_config SET price = #{price}, currency = #{currency}, update_time = NOW()
    WHERE id = #{id}
  </update>

</mapper>


